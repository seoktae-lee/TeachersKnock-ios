import SwiftUI

// ✨ 이름 충돌 방지를 위해 매니저 이름 변경 (SubjectName -> SubjectColorManager)
struct SubjectColorManager {
    
    // 🎨 컴파일러 오류 방지를 위해 'Color' 객체 대신 'Hex 코드 문자열'로 저장합니다.
    private static let paletteHex: [String] = [
        "#FF6B6B", // 00. 파스텔 레드
        "#4ECDC4", // 01. 민트
        "#45B7D1", // 02. 스카이 블루
        "#FFA07A", // 03. 살구색
        "#98D8C8", // 04. 페일 그린
        "#F7DC6F", // 05. 크림 옐로우
        "#B19CD9", // 06. 파스텔 퍼플
        "#FF9FF3", // 07. 캔디 핑크
        "#54A0FF", // 08. 브라이트 블루
        "#5F27CD", // 09. 딥 퍼플
        "#FF9F43", // 10. 오렌지
        "#10AC84", // 11. 다크 그린
        "#2E86DE", // 12. 스트롱 블루
        "#EE5253", // 13. 진한 레드
        "#0ABDE3", // 14. 시안
        "#1DD1A1", // 15. 라이트 에메랄드
        "#FECA57", // 16. 옐로우 오렌지
        "#8395A7", // 17. 그레이쉬 블루
        "#222F3E", // 18. 다크 네이비
        "#FD79A8"  // 19. 로즈 핑크
    ]

    // 과목 이름을 넣으면 고정된 색상을 반환하는 함수
    static func color(for subject: String) -> Color {
        let hash = abs(subject.hashValue)
        let index = hash % paletteHex.count
        
        // 기존에 프로젝트에 존재하는 init(hex:)를 사용합니다.
        return Color(hex: paletteHex[index])
    }
}
